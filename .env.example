# Optional: fallback API keys used when a project has no key set in Settings.
ANTHROPIC_API_KEY=
# Used for both text generation (if you pick OpenAI as your writing model)
# and cover art generation (gpt-image-1 / dall-e-3), which always uses
# OpenAI since Claude has no image-generation API.
OPENAI_API_KEY=
